<ul>
  <% @questions.order(created_at: :desc).each do |question| %>
    <li><%= link_to question.question_content, question_path(question) %> - <%= time_ago_in_words(question.created_at) %> ago</li>
  <% end %>
</ul>

<%= link_to "New Question", new_question_path %>
